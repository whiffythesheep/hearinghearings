.env
.venv/
# Written by run_audit.ps1 when the weekly audit finds something fixable;
# deleted automatically on the next clean run.
AUDIT-ACTION-REQUIRED.txt
__pycache__/
*.pyc
*.log
Input/
word_bank.json
# Assembled weekly digest HTML (ephemeral, pasted into MailerLite by hand)
weekly_digests/
# Ad-hoc screenshots dropped in the repo root for debugging
/*.jpeg
/*.jpg
/*.png

# Local X/Twitter brand assets (HTML sources for the gitignored PNGs)
/cover.html
/profile.html

# Claude Code local state (machine-specific: session ids, pids)
.claude/scheduled_tasks.lock
.claude/settings.local.json
